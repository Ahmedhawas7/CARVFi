{
  "name": "carvfi",
  "version": "1.0.0",
  "description": "Social Fi Platform on Carv SVM",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network carvTestnet",
    "test": "hardhat test"
  },
  "dependencies": {
    "@carv/sdk": "^1.0.0",
    "@openzeppelin/contracts": "^4.8.0",
    "ethers": "^5.7.0"
  },
  "devDependencies": {
    "hardhat": "^2.12.0"
  }
}
